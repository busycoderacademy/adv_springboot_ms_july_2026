K8S cheetsheet:
---------------

while (1) {kubectl get all;sleep 5;cls}
agenda:
-------
* Setup kind local cluster
* Pods
* Replicaset
* Deployment
* services
* namespace
* probes
* configMap and secreat
* persitance volume and statefullness
* HPA - Horizontal pod autoscaling
* ingress


What is cloud native application development?
--------------------------------------------
	an approach to develop mordern s/w (folling 12 factor/15 factor app)
	HA, Scalable, responsive, fault tolerent 
	speed ang agility

what is typically included in cloud-native applications
------------------------------------------------------
Cloud-native applications are designed specifically 
to run in cloud environments by leveraging cloud computing principles and infrastructure. 


Here’s a breakdown of what is typically included in cloud-native applications:
------------------------------------------------------------------------

Core Components of Cloud-Native Applications
----------------------------------------------

Microservices Architecture
	Application is broken into small, independent services.
	Each service is responsible for a single business capability 
	and can be deployed independently.

Containers
	Services are packaged in containers (e.g., Docker) 
	to ensure consistent environments across development, testing, and production.

Container Orchestration
	Tools like Kubernetes or OpenShift manage deployment, 
	scaling, and resilience of containerized applications.

DevOps and CI/CD Pipelines
	Continuous Integration and Continuous Delivery/Deployment 
	ensure faster and reliable release cycles.

	Tools: Jenkins, GitHub Actions, GitLab CI, ArgoCD, Tekton.

API-First Design
	Services communicate over lightweight APIs (REST, gRPC, GraphQL).
	Promotes loose coupling and easier integration.

Infrastructure as Code (IaC)
	Use of code to manage and provision infrastructure 
	(e.g., Terraform, AWS CloudFormation).

Cloud-Native Storage & Databases
	Use of scalable and resilient storage solutions:

	Block/Object storage (e.g., S3, Azure Blob)
	NoSQL (e.g., MongoDB, DynamoDB)
	Cloud-managed SQL (e.g., RDS, Cloud SQL)

Service Mesh
	Handles service-to-service communication, security, 
	and observability (e.g., Istio, Linkerd).

Auto-Scaling & Self-Healing
	Applications automatically scale based on load.
	Failed containers/services are automatically restarted.

Observability and Monitoring
	Centralized logging (ELK stack, Fluentd)
	Metrics and monitoring (Prometheus, Grafana, Datadog)
	Tracing (Jaeger, Zipkin)

Cloud Provider Integration
	Managed Services: Databases, message queues 
	(e.g., SQS, Pub/Sub), storage, serverless functions.

	Security & Identity: IAM, VPC, encryption, secrets management.

	Serverless Computing (Optional Part): AWS Lambda,
	 Google Cloud Functions, Azure Functions.

Example Tech Stack
---------------------
		Layer							Tools/Tech Examples
		---------------------------------------------------
		Programming Lang				Java, Go, Node.js, Python
		Frameworks						Spring Boot, Micronaut, Express.js
		Containerization				Docker
		Orchestration					Kubernetes
		CI/CD							GitHub Actions, Jenkins, ArgoCD
		Monitoring						Prometheus, Grafana, Loki, Fluentd
		Messaging						Kafka, RabbitMQ, NATS
		API Gateway						Kong, Istio, Ambassador, AWS API Gateway
		Storage							S3, EBS, Azure Blob, Google Cloud Storage
		Database						PostgreSQL, MongoDB, DynamoDB, Redis
		
https://github.com/mikeroyal/Cloud-Native-Guide?tab=readme-ov-file

		
Kubernetes (K8s) plays a key role in microservices by:
--------------------------------------------------------
	Orchestrating Containers 
			Automates deployment, scaling, and management of containerized microservices.

	Service Discovery & Load Balancing 
			Enables microservices to find and communicate with each other reliably.

	Auto-scaling
			Automatically adjusts the number of service instances based on traffic/load.

	Self-healing
		Restarts failed containers and replaces unresponsive ones automatically.

	Rolling Updates & Rollbacks
		Safely deploys new versions of services without downtime.

	Config & Secret Management
		Manages environment configs and secrets securely.
		
		
Docker pre req?
---------------
Docker arch, docker images, containers, networking, 
port mapping, volumens, exec, docker file etc


Docker vs k8s
---------------
	Docker provide OS level abstraction
	K8S provide infrastructure abs on cloud
	
What is docker?
-----------------
	open source plateform for packing the app + all dependencies + run time server
	seperate app from underlying host
	OS abstraction
	
What is kubernetes?
----------------------
	Kubernetes is an open-source container orchestration system for automating software deployment, 
	scaling, and management.
	
	Originally designed by Google, the project is now maintained by the Cloud Native Computing Foundation.
	 
	The name Kubernetes originates from Ancient Greek, meaning 'helmsman' or 'pilot'
	 
	container orchestration engine
	k8s manage life cycle of application
	cluster of nodes 
	admin : will manage the cluster
	dev   : will use tools to develop cloud native app
	
kubernetes which is provided by cloud provide

kubernetes setup:
-----------------
	local setup: docker + kubectl + minikube/kind
	
	cloud setup : 
	
	The four major Kubernetes providers are:
	--------------------------------------
	
	Google Kubernetes Engine (GKE): 
		Closely follows the latest changes in the Kubernetes open-source project;

	Azure Kubernetes Service (AKS): 
		Known for rich integration points to other Azure services
		
	Amazon Elastic Kubernetes Service (Amazon EKS): 
			One of the late players in the Kubernetes arena; a strong option due to AWS;

	DigitalOcean Kubernetes (DOKS): The new Kubernetes service in the market.

https://k21academy.com/docker-kubernetes/kubernetes-installation-options/

	
How to do local setup while learing k8s:
---------------------------------------
kind cluster config
1. install kubectl (client utilty to interact with k8s infra)
2. kind cluster / minikube



kubernetes Arch:
-----------------
	Master: also called control plan (1-more master for HA)
	Worker: 1 to many (5000) to make a cluster
	
components of k8s:
		Master node / control plan
		-----------------------------
		API server: 
			* Most important component, user intract with api server using imparative/decleartive commands
			* The API server is a component of the Kubernetes control plane that exposes the Kubernetes API. 
			* The API server is the front end for the Kubernetes control plane.
			
		etcd:
			* Consistent and highly-available key value store used as Kubernetes' 
				backing store for all cluster data.
				
		kube-scheduler:
			* Workload scheduler
			* It is Control plane component that watches for newly created Pods with no assigned node, 
				and selects a node for them to run on.
				
		kube-controller-manager: 
			* A process that continously monitor workload/nodes etc
			* desired state vs current state kubernetes
			
		Worker Node:
		-----------
			kubelet:
			* An agent that runs on each node in the cluster. 
			It makes sure that containers are running in a Pod.
			* It create and manage container inside a pod

			* The kubelet takes a set of PodSpecs that are provided through various mechanisms 
			and ensures that the containers described in those PodSpecs are running and healthy.
			 The kubelet doesn't manage containers which were not created by Kubernetes.

			kube-proxy:
			*kube-proxy is a network proxy that runs on each node in your cluster, 
			implementing part of the Kubernetes Service concept.

			* kube-proxy maintains network rules on nodes
			* handle conmmunication among the nodes within the cluster
			
			*These network rules allow network communication to your Pods from network sessions 
			inside or outside of your cluster.

			Container runtime:
			*A fundamental component that empowers Kubernetes to run containers effectively. 
			It is responsible for managing the execution and lifecycle of containers within the Kubernetes 				
			environment.

			Kubernetes supports container runtimes such as containerd, CRI-O,
			 and any other implementation of the Kubernetes CRI (Container Runtime Interface).


kubernetes working process:
---------------------------
1. API server expose API to interact with kubernetes (using Kubectl)

2. Workload is the application that we want to run.

	let assume we want to run 2 instances of nginx server
	
	developer write yaml file and send req to api server 
			|
	API server validate the request 
			|
	API server authenticate/authorize the request
			|
	API server store it into etcd db
	etcd container all info about cluster
			|
	API server talk to the scheduler if he can run
	2 intance of nginx
			|
	scheduler choose 2 worker node to run the workload and schedule it on worker node
			|
	controller manager responsible for continously monitoring
	the state of cluster.let assume one pod dies then, 
	controller manager find we need 2 instance and one is down
			|
	controller talk to API server
			|
	Scheduler pick another node to run the nginx instance
	
worker node how it works?

	As JRE is required to run java app
	in same way docker runtime is required to run container
	docker  is not required on each node (it is quite heavy) ContainerD
			|
			|
	let scheduler pick node1 and node2 to run 2 instance of nginx
			|
	API server inform kubelet (agent) (That is waiting for instruction of api server) to run 
	intance of app1 and app2 then kubelet will create and manage containers
	
	
tools for local development
---------------------------
kubectl : command line utlility to intract with API server
	 command line tool to intract with kubernete master / API server
	 
Kind:	kind is used to create local cluster 
	to setp a kubernetes cluster for development and testing
	kind vs minikube

verify installation:
	kind version
	kubectl version --output=yaml
	
Note: command remain same no matter what tool we are using :)



How to do local setup while learing k8s:
---------------------------------------
kind cluster config
1. install kubectl (client utilty to interact with k8s infra)
2. kind cluster


-----------------------
Setup kind local cluster
------------------------
docker system prune -af
docker network ls

ls ~/.kube/config

Step 1: create cluster configuration file

01-cluster.yaml
----------------
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: dev-cluster
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30001
    hostPort: 30001
    protocol: TCP
- role: worker
- role: worker


step 2: create kind cluster
-----------------------------
 kind create cluster --config .\01-cl.yaml  
 
 to delete cluster:
  kind delete cluster --name dev-cluster
  
 
 What it do?
 This will create local cluster with 3 nodes
 1 master and 2 worker node
 
 docker ps	
 	command will list nodes
 	dev-cluster-worker-1
 	dev-cluster-worker-2
 	dev-cluster-control-plan-3
 It will also create a bride network to conn bw different nodes
 
 
  cat ~/.kube/config
  will give information where the cluster, ip networking is running
  
 
  ~/.kube/config is the configuration file for kubectl. 
  
  It contains the cluster's API Server address, authentication details, and the active context. 
  kubectl reads this file before connecting to a Kubernetes cluster.
  
  
 Now kubectl able to talk to master check command

 kubectl version --output=yaml
 -------------
  get all notes in the cluster
  	
  kubectl get nodes
 
  to delete the cluster
  
  kind delete cluster --name dev-cluster
  
  
  Exploring kind cluster
  -----------------------
  docker ps
  
  4b649d988c08
  
  docker exec -it <cid> bash		now we are inside the master node
  
  docker exec -it 4b649d988c08 bash		now we are inside the master node
  
  cd /etc/kubernetes/manifests
  ls -l				will display all yaml file related to master node
  
  ps -aux				process running within control plan
  
  Now explore worker node:
 
   a07f40b83057
 
   docker exec -it a07f40b83057 bash
 


  -------------
   	Pods
  -------------
  A Pod is the smallest deployable unit in Kubernetes that runs one or more containers 
  sharing the same network and storage.
  
    * Pods are the smallest deployable units of computing that you can create and manage in Kubernetes.
	
  	* A Pod (as in a pod of whales or pea pod) is a group of one or more containers, 
	with shared storage and network resources, and a specification for how to run the containers
  	
  	* collection of containers that can run on k8s
  	
  	* workload is an application running on k8s cluster
  	
  	* Pod is basic building block to create workload
  	
  	* A pod can run one/ more containers
  	
  	* only one of the container contain app other container run as helper container
	
	
	Command K8s
	------------
	to create k8s cluster
	kind create cluster --config 01-cluster.yaml 

	to delete k8s cluster
	kind delete cluster --name dev-cluster

	get nodes in the cluster
	kubectl get nodes

	get pods in default ns
	kubectl get pod


Pod hello world:
 ---------------
   step 1: start cluster
   step 2: create yaml file
   step 3: run the command
   	
   Main Component of Pod manifest file:
   
   apiVersion:
   kind:
   metadata:
   spec:	
   	
01-pod.yaml 
---------------
apiVersion: v1
kind: Pod
metadata:
  name: productapp-pod
  labels:
    app: productapp
spec:
  containers:
  - name: productapp
    image: rgupta00/productapp:1.1
    ports:
    - containerPort: 8090

   
    kubectl create -f 00-pod.yaml
  
     kubectl get pod
    
	 kubectl describe pod
     give valuable informatation to debug pod
	 
	 while (1) {kubectl get all;sleep 5;cls}
	 

     
	kubectl get pod/productapp-pod
	kubectl get pod --show-labels

     kubectl delete -f 01-simple-pod.yaml --dry-run=client

 
Useful Debug Commands
----------------
kubectl get pods
kubectl get pods -o wide

Port forwarding
-----------------
	  
 create a tunnel from our machine to Pod
 -------------------------------------------
 port forwording: trick to check whether ur container is running successfully or not
 --------------
 kubectl port-forward pod/productapp-pod 8090:8090
 
Forwarding from 127.0.0.1:8090 -> 8090
Forwarding from [::1]:8090 -> 8090



Replicaset
----------
	A ReplicaSet's purpose is to maintain a stable set of replica Pods running at any given time. 
	As such, it is often used to guarantee the availability of a specified number of identical Pods.
	
	A ReplicaSet ensures that a specified number of pod replicas are running at any given time. 
	
	desired state vs current state
	
	Note: Deployment is a higher-level concept that manages ReplicaSets and provides declarative updates to
	 Pods along with a lot of other useful features. 
	 Therefore, we recommend using Deployments instead of directly using ReplicaSets
	 
	 

00-rs.yaml
-----------
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: productapp-rs
spec:
  replicas: 3

  selector:
    matchLabels:
      app: productapp

  template:
    metadata:
      labels:
        app: productapp

    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"

        env:
        - name: JAVA_TOOL_OPTIONS
          value: "-XX:MaxRAMPercentage=60 -XX:InitialRAMPercentage=50 -XX:+UseG1GC"

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15
		
		

kubectl create -f 00-rs.yaml
kubectl get rs
kubectl get all
kubectl get pod --show-labels

kubectl delete pod my-rs-j9k6l
new pod is create as we have mentioned desire count =3

kubectl delete rs productapp-rs
will delete replicaset




What happen if label is not matching pod label is not matching with Replicaset
---------------------------------------------------------------------------------
01-mismatch.yaml
-----------------
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: productapp-rs
spec:
  replicas: 3
#selector.matchLabels == template.metadata.labels
  selector:
    matchLabels:
      app: productapp-demo

  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"

        env:
        - name: JAVA_TOOL_OPTIONS
          value: "-XX:MaxRAMPercentage=60 -XX:InitialRAMPercentage=50 -XX:+UseG1GC"

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15


kubectl apply -f 01-mismatch.yaml

The ReplicaSet "productapp-rs" is invalid: spec.template.metadata.labels: 
Invalid value: map[string]string{"app":"productapp"}: `selector` does not match template `labels`

Note: You can have multiple labels but one must match







Deployment
=================
What is deployment?

	A Kubernetes Deployment tells Kubernetes how to create or modify instances of the pods that hold
	 a containerized application. 
	 
	 Deployments can help to efficiently scale the number of replica pods, 
	 enable the rollout of updated code in a controlled manner, 
	 or roll back to an earlier deployment version if necessary. 
	 
	 What are the benefits of using a Kubernetes Deployment?
	----------------------------------------------------------
	Kubernetes saves time and migrate errors by automating the work and 
	repetitive manual functions involved in deploying, scaling, and updating applications in production. 
	
	Since the Kubernetes deployment controller continuously monitors the health of pods and nodes,
	 it can make changes in real-time—like replacing a failed pod or bypassing down nodes—to ensure 
	 the continuity of critical applications.

	Deployments automate the launching of pod instances and ensure they are running as 
	defined across all the nodes in the Kubernetes cluster.
	
	Faster deployments with fewer errors.


	


Very Important:
---------------

  http://wiki.ciscolinux.co.uk/index.php/Kubernetes/Deployment,_ReplicaSet_and_Pod
   Deployement ----> replicaset --> pod
   
   
Annotations in kubernetes:
---------------------------
have special meaning for k8s
it is the way to pass some additional informationation using anntations

  annotations:
    kubernetes.io/change-cause: "deploying v3"
	
way to tell kubernete to show change cause when showing rollout history
kubectl rollout history deploy


Deployment example 1:
-------------------------
00-dep.yaml 
-----------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp
	  
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15
        
  
kubectl apply -f 00-dep.yaml 

kubectl get all

exec inside pod
kubectl exec -it pod/productapp-deploy-755fc86577-52t6x bash

kubectl get deployment

describe deployment:
kubectl describe deploy

getting logs of specific container that is inside a pod:
kubectl logs deploy/productapp-deploy		will show log of any one container that is running inside a pod
kubectl logs pod/productapp-deploy-5c5b7bc6d7-bvwpc


port forwarding:
kubectl port-forward deploy/productapp-deploy 8090:8090


kubectl describe pod
give valuable informatation to debug pod
	 



Kubernetes only supports two strategies in a Deployment:
------------------------------------------------------
	RollingUpdate
	Recreate

	Everything else (Canary, Blue-Green, A/B testing) is implemented using Kubernetes objects like Services,
	Ingress, or tools like Argo Rollouts / Istio.



Rolling Update Process
-----------------------
Kubernetes does not kill all pods at once. It replaces them gradually to avoid downtime.
v1.1  v1.2


4 pods -> 5

Default strategy:
-----------------
maxUnavailable: 25%
maxSurge: 25%

Why old pods disappeared
-----------------------
They were scaled down to zero by the Deployment controller after the new pods passed readiness checks.

Easy to Remember
-------------------
Rolling Update = Add new Pods one by one and remove old Pods one by one.

maxSurge = How many extra new Pods Kubernetes can create during the update.
maxUnavailable = How many Pods can be unavailable (down) at the same time.

Why did the old Pods disappear? 
	After the new Pods became Ready, the Deployment Controller scaled
	the old ReplicaSet down to 0, so the old Pods were deleted.
	In One Line

	Kubernetes creates a new Pod, waits until it is Ready,
	then removes one old Pod. 
	It repeats this process until all old Pods are replaced by new Pods.
	


	
01-rollout.yaml
---------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1
        imagePullPolicy: Always

        ports:
        - containerPort: 8090
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15
		  
		  	
kubectl apply -f 01-rollout.yaml

kubectl get all
--------------
exec inside pod
kubectl exec -it pod/my-deploy-5c5b7bc6d7-bvwpc bash

kubectl get deployment
--------------------
port forwarding
kubectl port-forward deploy/productapp-deploy 8090:8090

	  
	  
	  
Rollout history
---------------
	Switching from 1.1 to 1.2 
	
	change value from 1.1 --> 1.2 and change annoation text 
	
	kubectl port-forward deploy/productapp-deploy 8090:8090
	
	
kubectl rollout history deploy
--------------------------------
let assume there is bug in 1.2 and we want to migrate from 1.2 to 1.1

command:
------------
kubectl rollout undo deploy/productapp-deploy

if you want to see more details:
---------------------------------
kubectl rollout history deploy
kubectl rollout history deploy --revision=2

if we want to go to an specific version: (imp)
------------------------------------------------
kubectl rollout undo deploy/productapp-deploy --to-revision=2

now if we do port forwarding we switch version to previous version

port forwarding:
kubectl port-forward deploy/productapp-deploy 9090:8090


Min ready seconds:
------------------
	spring boot may take few second before it ready
	we can put this inforation in deployment file
	
	health check and probes: will be proper way to do this
	will be done latter


	
Note: default strategies : RollingUpdate
-----------------------------------------
Kubernetes automatically used RollingUpdate.

Default Strategy Configuration
--------------------------------
If you don’t define anything, Kubernetes internally assumes this:

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 25%
    maxSurge: 25%
	
What These Values Mean
-------------------------
maxSurge: 25%
Kubernetes can create extra pods above the desired replicas during an update.
	Your deployment:
	replicas: 3
	25% of 3 ≈ 1 pod

	So Kubernetes can temporarily run:
	3 desired + 1 extra = 4 pods
	
maxUnavailable: 25%
	Maximum number of pods that can be unavailable during the update.
	25% of 3 ≈ 1 pod
	So at least 2 pods must remain available.
	
	

RollingUpdate Strategy
----------------------
maxSurge
--------
maxSurge: 1
--------------
Meaning → Kubernetes can create 1 extra pod during rollout.

 spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
  
   Switching from v1 to v2 	
   kubectl rollout history deploy
   
Example:
replicas: 3
temporary pods: 4


maxSurge: "100%"
----------------
Meaning → Kubernetes can create up to 100% extra pods.

 spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: "100%"
	  
	  
Example:
replicas: 3
temporary pods: up to 6


minReadySeconds
---------------
spec:
  minReadySeconds: 10

Pod must stay READY for 10 seconds before being considered available.


maxUnavailable
--------------
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1

     here we are saying dont create extra pod it is ok if
	 one pod is not available during rolling update
	 
Meaning → No extra pods created, but 1 pod can be unavailable during rollout.


Default Behavior
----------------
maxSurge: 25%
maxUnavailable: 25%











	
Recreate strategy
-----------------
Recreate strategy, Kubernetes does a very simple but brutal thing:
	Kill all old pods first → then start the new pods.
	No gradual replacement. No overlap. Just a full stop and restart.

Why Recreate Exists
	Even though it causes downtime, it’s useful when two versions cannot run together.
	Database schema changes
	Shared file locks / singleton services
	Legacy stateful systems




02-recreate.yaml
-----------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: productapp
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1
        imagePullPolicy: Always
        ports:
        - containerPort: 8090
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15


		
strategy:
    type: Recreate
    
Switching from v1 to v2 	
kubectl rollout history deploy

In this strategy extra pod are not created
already existing pod first deleted and then new one are created
    

     
	 
	 

Kubernetes Service
===================

Pods are not stable network endpoints.
------------------------------------------

	Problems with Pods:
	------------------
		Pod IP changes when Pod restarts
		Pods are created and deleted frequently
		Client cannot track Pod IPs
		
		So Kubernetes introduces Service.
		A Service provides a stable network endpoint 
		that forwards traffic to Pods.
		
		Think of it as a load balancer in front of Pods.
		
		
Types of Services
------------------
ClusterIP (default)
-------------------
	Used for internal communication inside cluster.
	Not accessible from outside cluster.
	Pods call service using DNS:
		http://productapp-service:8090
		
	
NodePort
---------
	Exposes application outside cluster.
	Kubernetes opens a port on the node.
		Example:
		NodePort: 30001
		Access:
		http://localhost:30001
		http://productapp-service:8090

LoadBalancer
----------
	Used in cloud environments (AWS, Azure, GCP).
	Creates an external load balancer


NodePort
============
00-svc.yaml
-----------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1
        imagePullPolicy: Always

        ports:
        - containerPort: 8090
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15

---
apiVersion: v1
kind: Service
metadata:
  name: productapp-service

spec:
  type: NodePort
  selector:
    app: productapp

  ports:
  - port: 8091        # service port used inside the Kubernetes cluster
                      # cluster के अंदर दूसरी services इस service को 8091 port पर access करेंगी
    targetPort: 8090 # container port where the application runs
    nodePort: 30001  # external port used to access service from outside cluster
	
	

Let we decode what it means:
-------------------------
service/productapp-service NodePort 10.96.127.156 <none> 8091:30001/TCP

First understand the two ports
--------------------
8091:30001
This means:

SERVICE PORT : NODE PORT
------------------------
So:
Part	Meaning
----------------------
8091	Port used inside Kubernetes cluster
30001	Port opened on every Kubernetes Node



Complete path:
---------------
Browser
   ↓
NodeIP:30001     (NodePort)
   ↓
Service:8091     (Cluster service port)
   ↓
Pod:8091         (container port)



Why EXTERNAL-IP is <none>
-----------------------------
	Because NodePort does not create external load balancer.
	You must use:
	NodeIP:NodePort



Explanation:
------------
	port        = service port
	targetPort  = container port
	nodePort    = external port


Meaning of port: 8091
--------------------
port: 8091 means:
	Inside the Kubernetes cluster, other services or 
	pods will call this service using port 8091.

Example

If another service calls:

http://productapp-service:8091

Kubernetes will internally forward that request to the pod running the application.

How the request flows
---------------------
Service port 8091
       ↓
Pod targetPort 8090
       ↓
Spring Boot application
Simple mapping
Service port (8091)
       ↓
Pod targetPort (8090)
       ↓
Application

So other services talk to 8091, but the actual application inside the 
pod is listening on 8090. Kubernetes automatically forwards the request.



Explanation in Hindi 
-------------------------
port: 8091 का मतलब यह है:

क्लस्टर के अंदर बाकी services या pods
इस service को 8091 port पर call करेंगे।

यानि अगर कोई दूसरी service call करे:

http://productapp-service:8091

तो Kubernetes internally उसे आगे भेजेगा:

8091 (Service port)
        ↓
targetPort 8090 (Pod)
        ↓
Spring Boot application
Simple Mapping
Service port (8091)
        ↓
Pod targetPort (8090)
        ↓
Application





K8s Service — ClusterIP (default)
-----------------------------------

Concept
	A ClusterIP Service exposes an application only inside the Kubernetes cluster.
	External users cannot access it directly.
	Other pods communicate with the service using DNS name.
	Think of it like an internal company extension number 
	Employees inside the building can dial it, outsiders cannot.

Architecture
---------------
Pod (client)
     |
     v
Service DNS

productapp-service:8091
     |
     v
Service (ClusterIP)
     |
     v
Pod replicas
productapp:8090


What happens internally
-----------------------
demo-client pod
       |
       v
DNS resolves
productapp-service
       |
       v
ClusterIP
       |
       v
Load balanced request
       |
       v
productapp pods

Key Idea
-----------
Pods are ephemeral/emporary

	Their IP addresses change.
	Service provides a stable virtual IP 

	Pods communicate using:
	http://service-name:port



01-svc.yaml
------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp

  template:
    metadata:
      labels:
        app: productapp

    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1
        imagePullPolicy: Always

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15

---
apiVersion: v1
kind: Service
metadata:
  name: productapp-service

spec:
  type: ClusterIP

  selector:
    app: productapp

  ports:
  - port: 8091
    targetPort: 8090
---
apiVersion: v1
kind: Pod
metadata:
  name: demo-client

spec:
  containers:
  - name: demo
    image: rgupta00/util
    args:
    - "sleep"
    - "3600"



Test the service
--------------------
Enter demo pod:
	kubectl exec -it demo-client -- sh
	curl productapp-service:8091/hello-world





Blue-Green Deployment 
--------------------------
One-line Interview Answer

Blue-Green Deployment runs two identical Deployments (Blue and Green). 
The Service initially routes traffic to Blue. 

After Green is verified, we switch the Service selector from version=blue to version=green,
 instantly moving all traffic without downtime. 
If needed, rollback is done by changing the selector back to Blue.


Definition
------------
	Blue-Green deployment is a release strategy where two identical
	environments run at the same time:

BLUE  → current production version
GREEN → new version

Traffic is switched from Blue → Green after validation.

Core Idea
--------------
In Kubernetes this is implemented using:

	Two Deployments + different labels + Service selector switch

	Example labels:
	---------------
	app=productapp
	version=blue
	version=green

Service routes traffic to pods matching the selector.
-------------------------------------------
Traffic Flow

Initial State
------------
Client-------> Service --------> BLUE pods (v1)

After Deploying Green
-------------------
Client--------> Service -------->  BLUE pods (v1)

GREEN pods (v2)  ← running but not receiving traffic


After Traffic Switch
---------------------
Client -------->  Service --------> GREEN pods (v2)

Switch happens instantly by updating the Service selector.

Kubernetes Implementation
------------------------
Blue Deployment
	labels:
	  app: productapp
	  version: blue

Green Deployment
	labels:
	  app: productapp
	  version: green

Service decides which version receives traffic.
------------------------------------------------
selector:
  app: productapp
  version: blue

Switch to Green:
version: green

Advantages
------------
	Zero downtime
	Users experience no interruption.
	Instant rollback 
	Switch Service back to Blue.
	Safe production testing

Limitation
--------------
	Both environments run simultaneously, 
	so double resources are temporarily required.

One-line Interview Answer
--------------------------
Blue-Green deployment in Kubernetes is achieved by:

running two deployments (blue and green)
and switching the Service selector label




Deploy BLUE environment
-----------------------
kubectl apply -f .\01-blue-deploy.yaml
kubectl apply -f .\02-service.yaml
kubectl apply -f .\03-client.yaml

Check:
----------
kubectl get pods
kubectl get svc

Test application
---------------
Enter test pod:
kubectl exec -it demo-client -- sh

Call service:
curl productapp-service:8091/hello-world

Deploy GREEN version
------------------
kubectl apply -f .\04-green-deploy.yaml


Switch traffic to GREEN
---------------------
Edit service:
-----------
kubectl edit svc productapp-service

Change:
-----
version: blue
to
version: green


Quick way to verify
-----------------
kubectl get endpoints productapp-service
kubectl get pods -l version=green

kubectl exec -it demo-client -- sh
curl productapp-service:8091/hello-world

If the response is coming from green pods, the Blue→Green switch worked.

now delete all :
----------------
kubectl delete -f .
or
kubectl delete -f 01-blue-deploy.yaml
kubectl delete -f 02-service.yaml
kubectl delete -f 03-demo-client.yaml
kubectl delete -f 04-green-deploy.yaml






k8s Namespace
===================
A Kubernetes Namespace is a logical division of a cluster that allows multiple teams,
 applications, or environments to share the same cluster while keeping their resources isolated from each other.

Easy Example
-------------
Imagine Kubernetes is a big apartment building.

The building = Kubernetes Cluster
Each flat = Namespace
People in one flat don't interfere with people in another flat.

Kubernetes Cluster
│
├── Namespace: dev
│      ├── Pods
│      ├── Services
│      └── Deployments
│
├── Namespace: test
│      ├── Pods
│      ├── Services
│      └── Deployments
│
└── Namespace: prod
       ├── Pods
       ├── Services
       └── Deployments

Each namespace has its own resources.

Why do we use Namespaces?
---------------------------
Suppose three teams use the same cluster.
Development Team
Testing Team
Production Team

Without namespaces, all resources would be mixed together.

With namespaces:
--------------
dev Namespace
    product-app
    mysql
    redis

test Namespace
    product-app
    mysql
    redis

prod Namespace
    product-app
    mysql
    redis

The same resource names can exist in different namespaces without conflict.

Benefits
--------------
Organizes resources.
Isolates applications and teams.
Prevents name conflicts.
Allows separate access control (RBAC).
Enables resource limits and quotas for each namespace.
Common Commands


One-line Interview Answer
----------------------
A Kubernetes Namespace is a logical partition within a cluster that isolates and 
organizes resources, allowing multiple teams or environments to safely share the same Kubernetes cluster.




get all existing namespaces
----------------------------
kubectl get ns

NAME                 STATUS   AGE
default              Active   4h2m
kube-node-lease      Active   4h2m
kube-public          Active   4h2m
kube-system          Active   4h2m
local-path-storage   Active   4h2m


namespace started with kube are reserved for kubernetes cluster
kube-system  is super important
 
 to get all pods from all namespaces: (imp)
 kubectl get pod --all-namespaces
 

Kubernetes creates a few namespaces automatically:
---------------------------------------------
default – Used if you don't specify any namespace.

kube-system – Contains Kubernetes system components (CoreDNS, kube-proxy, etc.).

kube-public – Readable by all users; used for publicly accessible cluster information.

kube-node-lease – Stores node lease objects for efficient node heartbeats.




Creating namespace
----------------------
kubectl create ns dev
kubectl create ns qa


get pod in dev ns
-------------------
kubectl get pod -n dev

apply yaml config for dev ns
-------------------------
kubectl apply -f 01-ns-demo -n dev


get all resouces in dev ns
-------------------
kubectl get all -n dev

delete all resouces from dev ns
-------------------------
kubectl delete -f 01-ns-demo -n dev

delete all resources from qa namespace
----------------------------
kubectl delete ns qa

01-ns-demo.yaml
-----------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15


understanding kube-system 
-----------------------------
	kube-system is a special namespace in Kubernetes where it runs internal 
	system components that help the cluster work properly.

	Simple Explanation:
		Think of kube-system as the engine room of your Kubernetes cluster. 
		It holds the tools and services that keep Kubernetes itself running.

Examples of what runs in kube-system:
-----------------------------------
	kube-dns / CoreDNS – Helps services find each other (DNS).
	kube-proxy – Handles networking between pods.
	etcd – Stores cluster data and configuration.
	kube-controller-manager – Manages controllers (e.g., for replicas, nodes).
	kube-scheduler – Decides which node runs which pod.
	cloud-controller-manager (if using cloud provider).



kubectl get pod -n kube-system 
------------------------------
	it gives lods of pod running under kube-system as pod
	kindnet is CNI plugin to provide networking amongs different nodes
	CoreDNS is a dns server translate logical name to ip address

NAME                                                READY   STATUS    RESTARTS   AGE
coredns-5d78c9869d-jhzcl                            1/1     Running   0          4h12m
coredns-5d78c9869d-nq26f                            1/1     Running   0          4h12m
etcd-dev-cluster-control-plane                      1/1     Running   0          4h12m
kindnet-hbc26                                       1/1     Running   0          4h12m
kindnet-n9wkw                                       1/1     Running   0          4h12m
kindnet-p4vps                                       1/1     Running   0          4h12m
kube-apiserver-dev-cluster-control-plane            1/1     Running   0          4h12m
kube-controller-manager-dev-cluster-control-plane   1/1     Running   0          4h12m
kube-proxy-6t68m                                    1/1     Running   0          4h12m
kube-proxy-kql6j                                    1/1     Running   0          4h12m
kube-proxy-xjnxm                                    1/1     Running   0          4h12m
kube-scheduler-dev-cluster-control-plane            1/1     Running   0          4h12m






k8s probes:
----------------
Kubernetes Probes 
--------------------
When a container starts, Kubernetes assumes it is healthy unless probes say otherwise.
	Without probes:
		Container started → Pod considered Ready
		That can be dangerous because the application might still be starting.

Probes allow Kubernetes to verify:
-------------------------------------
	Is the container alive?  → Liveness Probe
	Is the container ready?  → Readiness Probe
	Is the container fully started? → Startup Probe


The logical execution order of Kubernetes probes is simple.
------------------------------------------------------
Startup → Readiness → Liveness

Startup Probe: Has the application finished starting?

Readiness Probe: Can this pod receive traffic?

Liveness Probe: Is the application still alive
				Is the application running correctly, or is it stuck/broken?

				Kubernetes periodically asks the container this question.

Understanding Liveness and readyness in day to day life
----------------------------------------------------
	Enter room
		  │
		  ▼
	Setup laptop
		  │
		  ▼
	Ready to teach
		  │
		  │  ← Readiness Probe passes
		  ▼
	Students enter
		  │
		  ▼
	Teaching starts
		  │
		  │  ← Liveness Probe keeps checking
		  ▼
	Teaching continues




1️. Readiness Probe
----------------
Purpose:
	etermine whether the pod can receive traffic.

If readiness fails:
	Service → stops sending traffic to that pod
	Pod is still running.

Typical use:
	Application still loading
	Database connection not ready
	Cache warming

Example:
----------
readinessProbe:
  httpGet:
    path: /actuator/health/readiness
    port: 8090
  initialDelaySeconds: 10
  periodSeconds: 10


Liveness Probe
-------------------
Purpose:
	Detect dead or stuck applications.

If liveness fails:
	Kubernetes restarts the container

Typical cases:
	Deadlock
	Memory corruption
	Application freeze

Example:
------------
livenessProbe:
  httpGet:
    path: /actuator/health/liveness
    port: 8090
  initialDelaySeconds: 30
  periodSeconds: 15
  
Meaning:
----------
	Check every 15s
	Restart container if unhealthy


Startup Probe 
--------------------
Purpose:
	Used for slow-starting applications.

Without startup probe:
	Liveness probe may kill container too early
	Startup probe disables liveness/readiness until startup completes.

Example
--------
startupProbe:
  tcpSocket:
    port: 27017
  periodSeconds: 1
  failureThreshold: 5

Meaning:

Try for 5 seconds
If Mongo not ready → restart



Probe Types
----------------
Kubernetes supports three probe mechanisms.

HTTP probe
-------------
httpGet:
  path: /health
  port: 8080

Most common for Spring Boot.

TCP probe
--------------
tcpSocket:
  port: 27017

Checks if port is open.

Used for databases.

Exec probe
-------------
exec:
  command:
  - cat
  - /tmp/healthy

Runs command inside container.

Use when HTTP endpoint does not exist.








01-prob.yaml
--------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: productapp-deploy
  annotations:
    kubernetes.io/change-cause: "deploy productapp v1.1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: productapp
  template:
    metadata:
      labels:
        app: productapp
    spec:
      containers:
      - name: productapp
        image: rgupta00/productapp:1.1

        ports:
        - containerPort: 8090

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8090
          initialDelaySeconds: 10
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8090
          initialDelaySeconds: 30
          periodSeconds: 15


port forwarding
kubectl port-forward deploy/productapp-deploy 8090:8090


02-startup-tcpsocket.yaml
------------------------
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
  - name: mongo
    image: mongo
    startupProbe:
      tcpSocket:
        port: 27017
      periodSeconds: 1
      failureThreshold: 5



03-startup-exec.yaml
----------------------
# prefer httpGet / tcpSocket
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
  - name: nginx
    image: nginx
    startupProbe:
      exec:
        command:
        - "cat"
        - "/usr/share/nginx/html/index.html"
      periodSeconds: 1
      failureThreshold: 3

# create a separate healthcheck
# /bin/healthcheck

      # exec:
      #   command:
      #   - "/bin/healthcheck"
	  






Configmap and secret:
----------------------
Containers should not hardcode configuration inside the image.

Example problems if config is inside the image:
	Changing a config → rebuild Docker image
	Same image cannot run in dev/qa/prod with different configs
	Secrets like passwords become visible

Kubernetes solves this using:
	ConfigMap → non-sensitive configuration
	Secret → sensitive configuration

Explanation of ConfigMap and Secret in Kubernetes:

ConfigMap vs Secret
---------------
ConfigMap
	Stores application configuration.
		Examples:
		feature flags
		API URLs
		message strings
		timeouts
		
Secret
	Stores sensitive information.
		Examples:
		database username
		database password
		API tokens
		private keys

Secrets are stored base64 encoded inside Kubernetes.

Example application:
-----------------------
how to encode username and password?
docker run --rm alpine sh -c "echo -n dbuser | base64"
docker run --rm alpine sh -c "echo -n dbuser | base64"


kubectl apply -f .
kubectl port-forward deploy/productapp-deploy 8090:8090
http://localhost:8090/config



Kubernetes uses base64?
-------------------------
Kubernetes requires secret values in base64 format because:
	YAML/JSON must store data as text
	Base64 safely represents binary or special characters

Important:
	In Kubernetes we typically store configuration in ConfigMaps
	and credentials in Secrets. 

	For higher security requirements, organizations integrate
	external secret managers like HashiCorp Vault so that
	applications fetch secrets dynamically instead of storing them
	in Kubernetes.

Instead of Spring Config Server many teams use:
------------------------------------------------
ConfigMap+ Secret + External config system (HashiCorp Vault)


Kubernetes stores Secrets in Base64-encoded format because YAML/JSON are text-based formats. 
Base64 is only an encoding mechanism, not encryption.

For stronger security,organizations use external secret managers like HashiCorp Vault, 
where applications fetch secrets dynamically instead of storing them directly in Kubernetes.


Demo 1: how to run
----------------------
kubectl apply -f .

Verify:
kubectl get pods


Example
productapp-deploy-5fdcc7b8fd-9dktp

Create Port Forward
kubectl port-forward pod/productapp-deploy-5fdcc7b8fd-9dktp 8080:8090




Demo 2: how to run
--------------------

Step 1: Create ConfigMap
-------------------------
kubectl apply -f 01-configmap.yaml

Verify:
kubectl get configmap

Output:
--------
NAME          DATA
demo-config   3

Show details:
--------------------
kubectl describe configmap demo-config

Output:
----------
database_host=192.168.0.1
debug_mode=1
log_level=verbose


"This ConfigMap stores three configuration values. My application doesn't know about them yet."

Step 2: Create Pod
------------------
kubectl apply -f 02-pod-envfrom.yaml

Check:
kubectl get pods

The Pod will complete quickly because it runs printenv.
-------------------------
NAME       READY   STATUS
demo-pod   0/1     Completed


"This Pod simply prints all environment variables and exits."

Step 3: View Logs
------------------------
kubectl logs demo-pod

We will see something similar to:
-------------------------
database_host=192.168.0.1
debug_mode=1
log_level=verbose
HOSTNAME=...
PATH=...
HOME=/root


Notice I never defined these variables in the Pod YAML. 
Kubernetes automatically imported all keys from the ConfigMap because I used envFrom."

Demo 2: Using configMapKeyRef
-------------------------
Delete the old Pod:
-------------------------
kubectl delete pod demo-pod

Apply the second Pod:
-------------------------
kubectl apply -f 03-pod-keyref.yaml

Again check:
-------------
kubectl get pods

Then:

kubectl logs demo-pod
-------------------------
Now we will see:

logging_mode=verbose
HOSTNAME=...
PATH=...
HOME=/root

Notice there is no:

database_host
debug_mode

This time I didn't import the whole ConfigMap. 
I only asked Kubernetes for one key: log_level.
Kubernetes created a new environment variable called logging_mode with the value verbose."




PV and PVC:
===============
If a database runs inside a Pod:
pod deleted → data lost

This is unacceptable for:
	databases
	uploaded files
	logs
	stateful systems

Kubernetes Solution: External Storage
--------------------------------------
Kubernetes stores data outside the Pod.

Think like this:
----------------
	Pod = compute
	Volume = storage

So if Pod dies:
	storage remains safe
When new Pod starts:
	it reconnects to same storage
	
Two Important Objects
----------------------
Kubernetes introduces two objects.
	PersistentVolume (PV)
	PersistentVolumeClaim (PVC)

PersistentVolume (PV)
------------------------
PV is actual storage in the cluster.
	It could be:
		local disk
		NFS
		AWS EBS
		Azure Disk
		Google Persistent Disk

Example concept:
	Hard disk = PersistentVolume

PV usually created by cluster admin.
	Example PV:
	10GB storage
	ReadWriteOnce
	
PersistentVolumeClaim (PVC)
------------------------
PVC is request for storage.
	Think of PVC like:
	"I need 2GB disk"

Kubernetes finds a PV that matches the request.
	Then binds them.

Simple Real World Analogy
----------------------
Think of a parking lot.

Parking Lot Space = PV
Your parking ticket = PVC
Your car = Pod

Flow:
Parking lot exists (PV)
You request parking spot (PVC)
Your car parks there (Pod)

How It Works Together
----------------------

The full flow:

	Admin creates PV
		   ↓
	Developer creates PVC
		   ↓
	Kubernetes binds PVC → PV
		   ↓
	Pod uses PVC
		   ↓
	Pod writes data to storage

How It Works Together
-----------------------
The full flow:

Admin creates PV
       ↓
Developer creates PVC
       ↓
Kubernetes binds PVC → PV
       ↓
Pod uses PVC
       ↓
Pod writes data to storage


Full Mental Model
---------------------

Application Pod
      │
      │ mounts storage
      ▼
PersistentVolumeClaim (PVC)
      │
      │ bound to
      ▼
PersistentVolume (PV)
      │
      │ actual storage
      ▼
Physical Disk / Cloud Storage







Persistent Volume (PV) and Persistent Volume Claim (PVC) in Kubernetes
------------------------------------------------------------------

What’s the Problem?
	When a container restarts in Kubernetes, 
	any files saved inside it are lost — like saving notes on a whiteboard
	that gets erased when the power goes out.

But you sometimes want permanent storage:
-------------------------------------
	Databases (MySQL, MongoDB)
	Uploaded files
	Logs

Kubernetes Solution: PV + PVC
--------------------------------

1. Persistent Volume (PV) = A physical storage space

	Think of it like a hard disk that lives outside the container.
	Can be local disk, NFS, AWS EBS, GCP Persistent Disk, etc.
	Created by admin or dynamically

2. Persistent Volume Claim (PVC) = A request for storage

	Like saying: “I need a 5GB disk that’s read-write.”
	Created by the developer or app
	Automatically gets matched with a suitable PV

Analogy:
-------------
Real Life												Kubernetes
Parking Lot (empty space)								PV (Persistent Volume)
You rent a spot											PVC (you claim it)
Your car (app) parks there								Pod uses the volume

How It Works Together
-----------------------
	Admin or cloud provider creates a PV
	Your app creates a PVC
	Kubernetes matches PVC to PV
	PVC is mounted to your Pod, and you get permanent storage



1-configMap.yml
--------------

kind: ConfigMap
apiVersion: v1
metadata:
  name: mysql-config
  labels:
    app: todo
data:
  MYSQL_DB: "todo-db"
  
2-secrets.yml
-------------
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  password: cm9vdA==
  

3-pv.yml
------
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-volume
  labels:
    type: mysql 
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/ubuntu/projects/mysql/volume"
	
	
4-pvc.yml
---------
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi


5-deployment.yml
--------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: MYSQL_DB
        volumeMounts:
        - name: mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-persistent-storage
        persistentVolumeClaim:
          claimName: mysql-pv-claim







Resource Management & Auto Scaling
-----------------------------------
how we can get cpu memory usages of pods?


Resource Management
-------------------
	Kubernetes allows you to control how much CPU and memory each pod can use:

	Requests – Minimum guaranteed resources.
	→ e.g., cpu: 100m, memory: 128Mi

	Limits – Maximum resources a pod can use.
	→ Prevents a pod from consuming too much.

	Helps with fair sharing, performance, and scheduling across the cluster.

Auto Scaling in K8s
--------------------
	Kubernetes can automatically adjust the number 
	of pods or resources based on load.

	Horizontal Pod Autoscaler (HPA)
		Scales pods in/out based on CPU usage or custom metrics.
		e.g., increase replicas when CPU > 80%

	Vertical Pod Autoscaler (VPA)
		Automatically adjusts CPU/memory requests/limits for a pod.

	Cluster Autoscaler
		Adds/removes nodes in the cluster based on pod demands
		(in cloud environments like AWS, GCP, Azure).

How It All Works Together:
-------------------------
	Requests/limits help schedule pods efficiently.
	HPA/VPA adjust pods/resources based on real-time usage.
	Cluster Autoscaler ensures enough infrastructure is available.



Step 1: install materix service:
-----------------------
kubectl apply -f .\metrics-server.yaml

check if matrix server is install and ready:
----------------------------------------------
kubectl get pod -n kube-system

Step 2: Deploy Spring Boot Application
----------------------------------------
kubectl apply -f .\dep.yaml

check pod is running:
kubectl get pods
kubectl get all

step 3:Verify Metrics Server
--------------------------
kubectl top nodes

step 4: Apply HPA
-------------------
kubectl apply -f .\hpa.yaml

and verify hpa:
---------------
kubectl get hpa

Verify Current Pods
---------------------
kubectl get pods

step 5: Generate CPU Load
---------------------
Run load generator pod
---------------------
kubectl run -i --tty load-generator --rm --image=busybox -- sh

Inside container run
------------------------
while true; do wget -q -O- http://productapp:8090/cpu; done

This will continuously hit the CPU endpoint.


step 6: Watch Autoscaling
-------------------------
kubectl get hpa -w

	Expected progression
	----------------------
	TARGETS   REPLICAS
	------------------
	20%/50%   1
	65%/50%   2
	75%/50%   3
	85%/50%   4
	90%/50%   5

Pods increase automatically.

step 7: Verify Pods
----------------

kubectl get pods

Expected
----------
5 pods running


Verify CPU Distribution
------------------------
kubectl top pods

Example
----------
productapp-deploy-xxxx   250m
productapp-deploy-yyyy   220m
productapp-deploy-zzzz   230m

Load distributed across pods.


step 8: Stop Load Generator
----------------------------

Exit busybox container
exit
After 2–5 minutes, check again
kubectl get hpa -w

Expected
-------
REPLICAS → gradually decrease

Finally
1 pod remains


Final Demo Flow
-------------
Start
1 pod

Generate CPU load
HPA detects >50%

Pods scale
1 → 2 → 3 → 4 → 5

Load stops
CPU drops

Pods scale down
5 → 1





Ingress:
---------
What is Ingress?
	Ingress is a Kubernetes resource that exposes your services to the internet 
	using a single IP or domain, often with path-based or domain-based routing.

It works with an Ingress Controller like NGINX to route HTTP/HTTPS traffic.

Step 1:

Nginx Ingress Controller:
--------------------------
The NGINX Ingress Controller is a Kubernetes component that uses 
NGINX act as a reverse proxy (with the help of Ingress controller) to manage external 
access to services in your cluster — typically over HTTP and HTTPS.

In Simple Terms:
---------------
	Think of it like a smart gatekeeper:
	You have many services in your Kubernetes cluster.

You want users to access them via clean URLs like:
	yourapp.com/api
	yourapp.com/login

Instead of exposing each service separately, 
you put one NGINX Ingress Controller in front, and it decides which service gets which request.


Architecture Overview:

User ──> Ingress Controller (NGINX) ──> Service A
                        └────────────> Service B
						
						
Lab step by step:
---------------------------

Step 1: Delete Cluster
kind delete cluster --name dev-cluster


Step 2: Create Cluster
kind create cluster --config 01-cluster.yaml


Step 3
-------
Check

kubectl get nodes

NAME                         STATUS
dev-cluster-control-plane    Ready
dev-cluster-worker           Ready
dev-cluster-worker2          Ready
Step 4

Step 4: Apply ingress controller
---------------------------
DO NOT USE main branch.Use a fixed version.

For Kubernetes 1.29 use:

kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.11.3/deploy/static/provider/kind/deploy.yaml

( v1.11.3 Kubernetes 1.29 tested)


This command installs the NGINX Ingress Controller in the Kubernetes cluster 
so that Ingress rules can route external HTTP/HTTPS traffic to Kubernetes Services.

what was installed?
---------------
Show all resources in the ingress-nginx namespace

kubectl get all -n ingress-nginx

Typical output:
----------
Pods
Services
Deployments
ReplicaSets
Jobs



Step 5: Wait
-----------
Wait until the NGINX Ingress Controller Pod is ready before deploying your applications.

kubectl wait --namespace ingress-nginx --for=condition=ready pod --selector=app.kubernetes.io/component=controller --timeout=180s



Step 6: Check
------
kubectl get pods -n ingress-nginx -o wide


Step 7: Deploy Application
--------------------
kubectl apply -f .


Step 8: Check
-------
kubectl get ingress
kubectl get svc
kubectl get pods
kubectl get endpoints

kubectl get ing

Step 9
------
Test: Browser

http://localhost

curl.exe http://127.0.0.1



One IMPORTANT change
------------------------
Add this line to Ingress YAML

apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: my-ingress

spec:
  ingressClassName: nginx

  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-app
            port:
              number: 80

Write ingressClassName explicitly 



Complete Flow
--------------------

Create KIND Cluster
        │
        ▼
Install NGINX Ingress Controller
        │
        ▼
Deploy Application + Service + Ingress
        │
        ▼
Ingress Rules Created
        │
        ▼
Client sends HTTP Request
        │
        ▼
NGINX Ingress Controller
        │
Chooses Service
        │
        ▼
Kubernetes Service
        │
Chooses Pod
        │
        ▼
Application Response


summary
--------
Delete Cluster → Start with a clean Kubernetes environment.

Create Cluster → Create a new KIND Kubernetes cluster.

docker ps → Verify that Kubernetes nodes are running as Docker containers.

Install NGINX Ingress Controller → Add the component that can process Ingress rules.

Deploy YAMLs → Create Deployments, Services, and Ingress resources.

Check Ingress → Verify that routing rules have been created.

curl localhost → Send a request and watch NGINX route it to the correct Kubernetes Service and Pod.





Resource Management & Auto Scaling
-----------------------------------
How we can get cpu memory usages of pods?


Resource Management
-------------------
	Kubernetes allows you to control how much CPU and memory each pod can use:

	Requests – Minimum guaranteed resources.
	→ e.g., cpu: 100m, memory: 128Mi

	Limits – Maximum resources a pod can use.
	→ Prevents a pod from consuming too much.

	Helps with fair sharing, performance, and scheduling across the cluster.

Auto Scaling in K8s
--------------------
	Kubernetes can automatically adjust the number 
	of pods or resources based on load.

	Horizontal Pod Autoscaler (HPA)
		Scales pods in/out based on CPU usage or custom metrics.
		e.g., increase replicas when CPU > 80%

	Vertical Pod Autoscaler (VPA)
		Automatically adjusts CPU/memory requests/limits for a pod.

	Cluster Autoscaler
		Adds/removes nodes in the cluster based on pod demands
		(in cloud environments like AWS, GCP, Azure).

How It All Works Together:
-------------------------
	Requests/limits help schedule pods efficiently.
	HPA/VPA adjust pods/resources based on real-time usage.
	Cluster Autoscaler ensures enough infrastructure is available.

kubectl top nodes


install materix service:
-----------------------
kubectl apply -f .\metrics-server.yaml


check if matrix server is install and ready:
----------------------------------------------

kubectl get pod -n kube-system

now try command to check cpu and memory usages:
--------------------------------------------

kubectl top nodes

NAME                        CPU(cores)   CPU%   MEMORY(bytes)   MEMORY%   
dev-cluster-control-plane   143m         0%     676Mi           5%        
dev-cluster-worker          37m          0%     234Mi           1%        
dev-cluster-worker2         32m          0%     180Mi           1%        
 
 kubectl apply -f .\01-deploy-cpu-memory-usage.yaml
 
 Now apply cpu usages:
 kubectl top pods

 now increse no of replica to 50, then run
 now some of the pod is in pending state
 
 kubectl describe pod/my-deploy-76d58cdd59-wvq5c
 
	   Warning  FailedScheduling  84s   
	   default-scheduler  0/3 nodes are available: 1 node(s) had untolerated taint
	   {node-role.kubernetes.io/control-plane: }, 2 Insufficient cpu. 
	   preemption: 0/3 nodes are available: 1 Preemption is not helpful for scheduling, 2 No preemption victims found for incoming pod.
	  
 
HPA template: Horizontal pod autoscaling
-------------
https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale-walkthrough/

hpa-template.yaml
----------------
it is monitoring the deployment and scale the pod the horizontally automatically
till now we are manually update no of pod
but we want to scale automatically in weekday less and in weekend is more


HorizontalPodAutoscaler: used to monitor deployment


kubectl apply -f 02-deploy-for-hpa.yaml

kubectl apply -f 03-hpa.yaml

kubectl get all

kubectl get hpa

kubectl exec -it demo-pod -- bash

 ab -n 2000 -c 5 http://ngnix/





Spring boot k8s graphana promothesis integration:
------------------------------------------


0. Clean Environment (optional but good practice)
-----------------------------------------------
kubectl delete all --all
kubectl get all


2. Apply deployment + service YAML
----------------------------------
kubectl apply -f dep.yaml

kubectl get pods
kubectl get svc

Port-forward:
kubectl port-forward service/productapp 8090:8090

Open browser:
http://localhost:8090/actuator/prometheus

Expected:
You should see lots of metrics text, for example:
jvm_memory_used_bytes
http_server_requests_seconds

This proves Spring Boot metrics are exposed.
Stop port-forward (Ctrl+C).

 Install Prometheus + Grafana
-----------------------------------
Add Helm repo:

helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

Install monitoring stack:
helm install monitoring prometheus-community/kube-prometheus-stack

Expected:

NAME: monitoring
STATUS: deployed

6 Wait for Monitoring Pods
---------------------------
Check pods:

kubectl get pods

Expected pods:

monitoring-grafana-xxxxx
monitoring-prometheus-xxxxx
monitoring-kube-state-metrics

All should be Running.

7 Get Grafana Password (Windows command)
-----------------------------------
Run:

kubectl get secret monitoring-grafana -o jsonpath="{.data.admin-password}" | % { [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($_)) }

Expected output:

Abc123xyz

That is the Grafana password.

8 Access Grafana
------------------
Run:

kubectl port-forward deployment/monitoring-grafana 3000:3000

Open browser:

http://localhost:3000

Login:

username: admin
password: <password from step 7>
9 Create ServiceMonitor

Create file:

productapp-monitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: productapp-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: productapp
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 15s

Apply it:

kubectl apply -f productapp-monitor.yaml

Expected:

servicemonitor.monitoring.coreos.com/productapp-monitor created
10 Verify Prometheus Discovery

Port-forward Prometheus:

kubectl port-forward svc/monitoring-kube-prometheus-prometheus 9090

Open:

http://localhost:9090

Go to:

Status → Targets

Expected:

productapp-monitor   UP
11 Test Prometheus Query

In Prometheus UI run:

jvm_memory_used_bytes

Expected:

graph with JVM metrics

If data appears → scraping works.

12 Import Grafana Dashboard

In Grafana:

Dashboards → Import

Use dashboard ID:

4701

This is Micrometer JVM dashboard.

Expected graphs:

JVM Heap Memory
HTTP Requests
CPU Usage
Threads
GC Activity
Final Architecture
Spring Boot App
      │
      ▼
Service
      │
      ▼
ServiceMonitor
      │
      ▼
Prometheus
      │
      ▼
Grafana Dashboard
What You Achieved

You now have:

Spring Boot metrics
Prometheus scraping
Grafana dashboards
Kubernetes monitoring

This is real production-style observability architecture.



















ref:
understanding deployment strategies
https://medium.com/devops-dev/kubernetes-deployment-strategies-part-1-f4a7b959c5e5

https://www.devopsschool.com/blog/category/kubernetes/

https://www.harness.io/blog/kubernetes-services-explained





kind download:
--------------

curl.exe -Lo kind-windows-amd64.exe https://kind.sigs.k8s.io/dl/v0.26.0/kind-windows-amd64
Move-Item .\kind-windows-amd64.exe C:\setup_k8s\kind.exe



download minikube:
-------------------
New-Item -Path 'c:\setup_k8s' -Name 'minikube' -ItemType Directory -Force
Invoke-WebRequest -OutFile 'c:\setup_k8s\minikube.exe' -Uri 'https://github.com/kubernetes/minikube/releases/latest/download/minikube-windows-amd64.exe' -UseBasicParsing


https://github.com/kubernetes/minikube/releases



     
